server {
    listen ${PORT};
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA 라우팅: 새로고침/직접 URL 접근 시 404 방지 (Vue Router history 모드)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
